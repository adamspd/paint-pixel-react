/*
const User = require('../data/users');
const PixelBoard = require('../data/PixelBoards');

const createUsers = async () => {
    
    const firstUser = await User.create({
        username: "firstUser",
        password: "password"

    });
    const secondUser = await User.create({
        username: "secondUser",
        password: "password"

    });
    const thirdUser = await User.create({
        username: "thirdUser",
        password: "password"

    });

};

const createPixelBoards = async () => {

    const firstPixelBoard = await PixelBoard.create({
        title: "firstPixelBoard",
        boardSize: 2,
        pixelModification: true,
        timeLimit: 10,
        
    });
    const secondPixelBoard = await PixelBoard.create({
        title: "secondPixelBoard",
        boardSize: 5,
        pixelModification: true,
        timeLimit: 100,
        
    });
}

module.exports = {createUsers, createPixelBoards}*/
